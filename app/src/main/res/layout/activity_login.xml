<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.bukantkpd.bukabareng.LoginActivity">
    
    <com.bukantkpd.bukabareng.EdittextMontserrat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:id="@+id/password_view"
        android:inputType="textPassword"
        android:background="@drawable/edit_text_shape"
        android:hint="password"
        android:textSize="20dp"
        android:padding="15dp"/>

    <com.bukantkpd.bukabareng.EdittextMontserrat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/edit_text_shape"
        android:hint="username"
        android:textSize="20dp"
        android:padding="15dp"
        android:id="@+id/username_view"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/password_view"
        android:layout_marginBottom="15dp"
        />
    <com.bukantkpd.bukabareng.ButtonMontserrat
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Login"
        android:layout_below="@id/password_view"
        android:layout_marginTop="15dp"
        android:id="@+id/login_button_view"/>

</RelativeLayout>
